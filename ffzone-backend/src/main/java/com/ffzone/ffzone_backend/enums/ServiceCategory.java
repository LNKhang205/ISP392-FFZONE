package com.ffzone.ffzone_backend.enums;

// Danh mục dịch vụ (category) — nhiều Service có thể cùng 1 category,
// KHÁC với version cũ (service_type là UNIQUE, chỉ 1 bản ghi/loại).
public enum ServiceCategory { DRINK, EQUIPMENT, FACILITY }
